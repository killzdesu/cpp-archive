#include<stdio.h>
#include<algorithm>
#include<vector>

using namespace std;

int s[6000], i, n, k, m, N, M, K;

int main(){
int a, b, c, d;
scanf("%d %d %d", &N ,&M, &K);
for(a=0;a<N;a++){
    scanf("%d", &s[a]);
    if(a) s[a] += s[a-1];
}
for()



return 0;
}
